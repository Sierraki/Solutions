class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        digits = "".join(map(str, digits))
        digits = int(digits) + 1
        L1 = [int(a) for a in str(digits)]
        return L1
