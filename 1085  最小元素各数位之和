class Solution:
    def sumOfDigits(self, nums: List[int]) -> int:
        nums.sort()
        a = str(nums[0])
        su = 0
        for i in a:
            su = su + int(i)
        if su % 2 == 1:
            return 0
        else:
            return 1
